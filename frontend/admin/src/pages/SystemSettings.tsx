import React from 'react';
import { Typography } from 'antd';

const { Title, Paragraph } = Typography;

const SystemSettings: React.FC = () => {
  return (
    <div>
      <Title level={2}>系统设置</Title>
      <Paragraph>此页面正在开发中，即将推出...</Paragraph>
    </div>
  );
};

export default SystemSettings; 